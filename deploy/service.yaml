apiVersion: v1
kind: Service
metadata:
  name: homelab-api
  labels:
    app.kubernetes.io/name: homelab-api
spec:
  selector:
    app.kubernetes.io/name: homelab-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
