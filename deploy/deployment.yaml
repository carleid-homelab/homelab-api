apiVersion: apps/v1
kind: Deployment
metadata:
  name: homelab-api
  labels:
    app.kubernetes.io/name: homelab-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: homelab-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: homelab-api
    spec:
      containers:
        - name: api
          image: ghcr.io/carleid-homelab/homelab-api:latest
          ports:
            - containerPort: 8080
          env:
            - name: GRAFANA_TOKEN
              valueFrom:
                secretKeyRef:
                  name: homelab-api-secrets
                  key: grafana-token
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              memory: 64Mi
